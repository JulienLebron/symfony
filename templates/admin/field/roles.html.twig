{% block _user_roles %}

{% dump %}

    {% for role in field.value %}

        {% if role == 'ROLE_USER' %}
            Membre
        {% elseif role == 'ROLE_ADMIN' %}
            Admin 
        {% else %} 
            Modérateur
        {% endif %}

    {% endfor %}

{% endblock %}